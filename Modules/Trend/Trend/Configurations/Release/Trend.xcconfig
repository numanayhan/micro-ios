PRODUCT_BUNDLE_IDENTIFIER = com.na.release
BUNDLE_ID = $(PRODUCT_BUNDLE_IDENTIFIER)
CODE_SIGN_IDENTITY=iPhone Developer
PROVISIONING_PROFILE_SPECIFIER = match Development com.na.release
PROVISIONING_PROFILE = $(sigh_${BUNDLE_ID}_app_store)
//DEVELOPMENT_TEAM = XXXXXXXX

// For Firebase Analytics
OTHER_LDFLAGS = $(inherited) -ObjC
